version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: latest
  build:
    commands:
      # trigger the unit tests here
      - echo 'Install Dependencies & Run Test Case'
      - cd get-message-handler && npm i && npm run test

